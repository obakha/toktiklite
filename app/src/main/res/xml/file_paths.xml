<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Where the camera intent used by the WebView file chooser writes its capture before
         handing the content:// Uri back to the page (e.g. TikTok's "take photo" upload option). -->
    <cache-path name="captures" path="captures/" />
</paths>
