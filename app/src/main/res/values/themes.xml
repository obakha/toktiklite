<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. Material3 DayNight so the system dark-mode setting is honored
         automatically; explicit day/night color overrides live in values / values-night. -->
    <style name="Theme.TokTikLite" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/brand_accent_dark</item>
        <item name="colorOnPrimary">@color/on_surface_light</item>
        <item name="colorSurface">@color/surface_light</item>
        <item name="colorOnSurface">@color/on_surface_light</item>
        <item name="colorOnSurfaceVariant">@color/on_surface_light</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar" tools:targetApi="23">true</item>
        <item name="android:windowLightNavigationBar" tools:targetApi="27">true</item>
    </style>

    <!-- Shown between process start and the first WebView frame. Kept intentionally minimal:
         a plain background + the launcher icon, per the Android 12+ SplashScreen API contract. -->
    <style name="Theme.TokTikLite.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/surface_light</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>
        <item name="postSplashScreenTheme">@style/Theme.TokTikLite</item>
    </style>

</resources>
